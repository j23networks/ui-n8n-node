# Dependencies & build output
node_modules/
dist/
package-lock.json

# Local env / secrets (not used by the node itself — defensive only)
.env
.env.*
!.env.example

# npm packaging
*.tgz

# Local Claude Code settings (machine-specific permissions)
.claude/settings.local.json

# Logs & OS cruft
*.log
.DS_Store
